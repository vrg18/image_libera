name: image_libera
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0-0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.9.0

flutter:
  uses-material-design: true

  assets:
    - res/doctor-who.jpg

flutter_icons:
  android: launcher_icon
  ios: true
  web: true
  image_path: res/doctor-who.jpg
